$$  коментарий
$tip114    <----- тип вопроса
$wo1 -------------------------------------------------
   

      При использовании ДОФМ (двойной ОФМ) …
$ot1 ------------------------------------------------

+    каждые два бита сигнала данных (дибит)
    кодируются одним линейным сигналом.

     каждые два бита сигнала данных (дибит)
    кодируются двумя линейными сигналами.

 
     каждый бит сигнала данных кодируется
    одним линейным сигналом.

     каждый бит сигнала данных кодируется
    двумя линейными сигналами.
$ot2 ------------------------------------------------
 
     каждый бит сигнала данных кодируется
    двумя линейными сигналами.

+    каждые два бита сигнала данных (дибит)
    кодируются одним линейным сигналом.


     каждые два бита сигнала данных (дибит)
    кодируются двумя линейными сигналами.
 
     каждый бит сигнала данных кодируется
    одним линейным сигналом.
$ot3 ------------------------------------------------
 
     каждый бит сигнала данных кодируется
    одним линейным сигналом.

     каждый бит сигнала данных кодируется
    двумя линейными сигналами.

+    каждые два бита сигнала данных (дибит)
    кодируются одним линейным сигналом.


     каждые два бита сигнала данных (дибит)
    кодируются двумя линейными сигналами.

$ot4 ------------------------------------------------
 
     каждые два бита сигнала данных (дибит)
    кодируются двумя линейными сигналами.


     каждый бит сигнала данных кодируется
    одним линейным сигналом.

     каждый бит сигнала данных кодируется
    двумя линейными сигналами.

+    каждые два бита сигнала данных (дибит)
    кодируются одним линейным сигналом.
$spr*  <- справка
 *****  


$end   <--- конец справки и всего файла


