> м. т. - материальная точка
# Импульс системы м. т.
$$
\begin{flalign}
	& \vec P_{с} = \sum \vec p_i = \sum m_i \vec v_i 
	& & \\
\end{flalign}
$$
### Закон изменения импульса системы м. т.
$$
{d\vec P_{с} \over dt} = \sum \vec F_i^{внешн}
$$
### Закон сохранения импульса системы м. т.
$$
\begin{flalign}
	& \boxed{\sum \vec F_i^{внешн} = {d\vec P_{с} \over dt} = 0 \implies \vec P_{с} = \text{const}} \\
	& & \\
\end{flalign}
$$
#### Следствия
* Если система **замкнута** (силы извне не действуют) или внешние силы **компенсируют** друг друга, то:
	* $\vec P_{с} = \text{const}$
	* $\vec V_{цм} = \text{const}$ Скорость центра масс постоянна, т. е. система **неподвижна**, либо двигается с постоянной скоростью **без ускорения**
* Если система **замкнута** частично (по одной оси, например $x$), то $\vec P_{сx} = \text{const}$
# Центр масс системы материальных точек
$$
\begin{flalign}
	&
	\boxed{
		\begin{flalign}
			& \vec R_{цм} = {\sum m_i \vec r_i \over \sum m_i} - \text{Радиус-вектор центра масс} \\
			& \vec r_i - \text{Радиус-вектор отдельной материальной точки} \\
			& m_i - \text{Масса отдельной материальной точки} \\
		\end{flalign}
	}
	&
	\\
\end{flalign}
$$
В координатной форме:
$$
\begin{flalign}
	& X_{цм} = {\sum m_i x_i \over \sum m_i} - \text{x координата центра массс} \\
	& Y_{цм} = {\sum m_i y_i \over \sum m_i} - \text{y координата центра массс} \\
	& Z_{цм} = {\sum m_i z_i \over \sum m_i} - \text{z координата центра массс} \\
	& x_i,\ y_i,\ z_i  - \text{x, y, z координаты отдельной точки} \\
	& &
\end{flalign}
$$
### Закон динамики для центра масс
$$
\begin{flalign}
	&
	\boxed{
		\sum m_i \vec a_{цм} = \sum \vec F_i^{внешн}
	}
	&
\end{flalign}
$$
#### Следствия:
* Если система **замкнута** (силы извне не действуют) или внешние силы **компенсируют** друг друга, то $a_{цм} = 0 \implies v_{цм} = \text{const}$
# Момент импульса системы материальных точек
Векторное произведение:
![[Векторное произведение.png]]
$$
\begin{flalign}
	&
	\boxed{
		\begin{flalign}
			& {\color{NavyBlue}\vec c} = {\color{red}\vec a} \times {\color{lightgreen}\vec b} \implies 
			\begin{cases}
				{\color{NavyBlue} c} = |{\color{NavyBlue}\vec c}| =|{\color{red}\vec a}| \cdot |{\color{lightgreen}\vec b}| \cdot \sin({\alpha}) \\
				{\color{NavyBlue}\vec c} \perp {\color{red}\vec a} \\
				{\color{NavyBlue}\vec c} \perp {\color{lightgreen}\vec b}
			\end{cases}
			&
		\end{flalign}
	}
	&
\end{flalign}
$$

$$
\begin{flalign}
	& \vec l = \vec r \times \vec p - \text{Момент импульса материальной точки} \\
	& \vec L = \sum \vec l_i - \text{Момент импульса системы материальных точек} \\
\end{flalign}
$$
### Закон изменения момента импульса системы материальных точек
$$
{d\vec L \over dt} = \sum \vec r_i \times \vec F_i
$$
# Ответы на вопросы
